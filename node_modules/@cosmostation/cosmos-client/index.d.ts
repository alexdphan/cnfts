import type { OfflineSigner } from '@cosmjs/proto-signing';
import WalletConnect from '@walletconnect/client';
import { ExtensionInstallError } from './error';
export { ExtensionInstallError };
export interface CosmostationAccount {
    address: Uint8Array;
    algo: string;
    bech32Address: string;
    isNanoLedger: boolean;
    name: string;
    pubKey: Uint8Array;
}
export declare const getExtensionOfflineSigner: (chainId: string) => Promise<OfflineSigner>;
export declare const connectWallet: () => Promise<WalletConnect>;
export declare const getMobileOfflineSignerWithConnect: (chainId: string) => Promise<OfflineSigner>;
export declare const getOfflineSigner: (chainId: string) => Promise<OfflineSigner>;
